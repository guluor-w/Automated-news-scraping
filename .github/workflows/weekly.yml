name: Weekly AI Policy Collector

on:
  schedule:
    - cron: "0 4,17 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Shanghai
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m playwright install chromium --with-deps

      - name: Run collector
        id: collector
        run: |
          python src/collect.py
          echo "added=$(cat docs/data/added_count.txt )" >> "$GITHUB_OUTPUT"

      - name: Commit and push (only if new rows)
        if: steps.collector.outputs.added != '0'
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: weekly policy news update"
          file_pattern: docs/data/policy_news.csv  docs/data/added_count.txt  docs/data/rss_full.xml  docs/data/rss_miit.xml
